#include <stdio.h>
#include 
int recupererNombre(int minimum, int maximum){
    int nombre;
    do{
        printf("Entrez un nombre compris entre %d et %d :",minimum,maximum);
        scanf("%d",&nombre);
        if (nombre<minimum){

        }
    }while(1);
}
int main(int argc, char* argv[])
{
   // printf() displays the string inside quotation
   printf("Hello, World!");
   return 0;
   printf("%")
}